<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="1dp">

    <com.anant.mediacurator.SquareImageView
        android:id="@+id/ivPhoto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:background="#DDDDDD"
        android:contentDescription="Media thumbnail" />

    <!-- Gradient overlay at bottom for duration text visibility -->
    <View
        android:id="@+id/bottomGradient"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bottom_gradient"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/ivTypeIcon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center"
        android:background="@drawable/type_icon_bg"
        android:padding="6dp"
        android:src="@android:drawable/ic_media_play"
        app:tint="@android:color/white"
        android:visibility="gone" />

    <!-- Date badge (top-left): shown only in flat SIZE_ABSOLUTE mode.
         Lives at the top so it has the full cell width and never competes
         with the size label at the bottom. -->
    <TextView
        android:id="@+id/tvDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="4dp"
        android:paddingHorizontal="5dp"
        android:paddingVertical="2dp"
        android:background="#BB000000"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:singleLine="true"
        android:visibility="gone" />

    <!-- Size / duration (bottom-right): always shown -->
    <TextView
        android:id="@+id/tvDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="4dp"
        android:paddingHorizontal="4dp"
        android:paddingVertical="1dp"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:singleLine="true"
        android:visibility="gone" />

    <View
        android:id="@+id/selectionOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#66000000"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/ivCheckmark"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="top|end"
        android:layout_margin="4dp"
        android:src="@android:drawable/checkbox_on_background"
        android:visibility="gone" />
</FrameLayout>